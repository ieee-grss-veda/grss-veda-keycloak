---
# Base master realm configuration
# IDP and auth flows are defined in environment-specific files (master.dev.yaml, master.staging.yaml, etc.)
enabled: true
realm: master
displayName: Admin
displayNameHtml: VEDA Keycloak Admin

loginTheme: veda
loginWithEmailAllowed: false
