name: Deploy & Configure Service

on:
  workflow_call:
    inputs:
      environment:
        required: true
        description: "Environment to deploy to"
        type: string
  workflow_dispatch:
    inputs:
      environment:
        required: true
        description: "Environment to deploy to"
        type: environment

permissions:
  id-token: write # Required for OIDC authentication w/ AWS
  contents: read

concurrency:
  group: ${{ inputs.environment }}
  cancel-in-progress: false

run-name: Deploy to ${{ inputs.environment }} environment by @${{ github.actor }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: 
      name: ${{ inputs.environment }}
      url: ${{ vars.HOSTNAME }}

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_CDK_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
          role-session-name: GitHubActionsCDKDeploy
          role-duration-seconds: 3600

      - name: Set up Python 3.13
        uses: actions/setup-python@v4
        with:
          python-version: "3.13"

      - uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync -p 3.13

      - name: Deploy CDK to ${{ inputs.environment }} environment
        run: |
          uv run npx cdk deploy --require-approval never --outputs-file outputs.json
        env:
          AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
          AWS_REGION: ${{ vars.AWS_REGION }}
          CDK_BOOTSTRAP_QUALIFIER: ${{ vars.CDK_BOOTSTRAP_QUALIFIER }}
          VPC_ID: ${{ vars.VPC_ID }}
          PERMISSIONS_BOUNDARY_ARN: ${{ vars.PERMISSIONS_BOUNDARY_ARN }}
          HOSTNAME: ${{ vars.HOSTNAME }}
          KEYCLOAK_VERSION: ${{ vars.KEYCLOAK_VERSION }}
          KEYCLOAK_CONFIG_CLI_VERSION: ${{ vars.KEYCLOAK_CONFIG_CLI_VERSION }}
          SSL_CERTIFICATE_ARN: ${{ vars.SSL_CERTIFICATE_ARN }}
          STAGE: ${{ inputs.environment }}
          # Imported Identity Provider secrets (GitHub OAuth - for dev)
          IDP_SECRET_ARN_GH: ${{ vars.IDP_SECRET_ARN_GH }}
          IDP_SECRET_ARN_VEDA_GH: ${{ vars.IDP_SECRET_ARN_VEDA_GH }}
          # SAML Identity Provider secrets (for staging/prod)
          SAML_SECRET_ARN: ${{ vars.SAML_SECRET_ARN }}
          VEDA_SAML_SECRET_ARN: ${{ vars.VEDA_SAML_SECRET_ARN }}
          RDS_SNAPSHOT_IDENTIFIER: ${{ vars.RDS_SNAPSHOT_IDENTIFIER }}
          CONFIGURE_ROUTE53: ${{ vars.CONFIGURE_ROUTE53 }}
          HOSTED_ZONE_DOMAIN: ${{ vars.HOSTED_ZONE_DOMAIN }}
          # Resource sizing (optional overrides - defaults to lower resources for dev/staging)
          ECS_CPU: ${{ vars.ECS_CPU }}
          ECS_MEMORY_MIB: ${{ vars.ECS_MEMORY_MIB }}
          RDS_INSTANCE_CLASS: ${{ vars.RDS_INSTANCE_CLASS }}
          RDS_INSTANCE_SIZE: ${{ vars.RDS_INSTANCE_SIZE }}
          ECS_HEALTH_CHECK_GRACE_PERIOD: ${{ vars.ECS_HEALTH_CHECK_GRACE_PERIOD }}

      - name: Get outputs from CloudFormation
        id: get-outputs
        run: |
          STACK_KEY="veda-keycloak-${{ inputs.environment }}"
          echo "CONFIG_LAMBDA_ARN=$(jq -r ".\"$STACK_KEY\".ConfigLambdaArn" outputs.json)" >> $GITHUB_ENV
          echo "CLUSTER_NAME=$(jq -r ".\"$STACK_KEY\".ClusterName" outputs.json)" >> $GITHUB_ENV
          echo "SERVICE_NAME=$(jq -r ".\"$STACK_KEY\".ServiceName" outputs.json)" >> $GITHUB_ENV

      - name: Wait for Keycloak service to stabilize
        run: |
          echo "Waiting for ECS service $SERVICE_NAME in cluster $CLUSTER_NAME to stabilize..."
          aws ecs wait services-stable --cluster "$CLUSTER_NAME" --services "$SERVICE_NAME"
          echo "Keycloak service is stable."

      - name: Apply Config
        run: uv run bin/apply-config.py $CONFIG_LAMBDA_ARN $(echo '${{ toJSON(vars) }}' | jq -c .)
